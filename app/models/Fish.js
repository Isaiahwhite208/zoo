class Fish {
 constructor(
  name,
  gender,
  age,
  color,
  size,
  weight,
  species,
 ) {
  this.id = Math.random()
  this.id = name || ''
  this.id = gender || ''
  this.id = age || ''
  this.id = color || ''
  this.id = size || ''
  this.id = weight || ''
  this.id = species || ''
 }
}

export default Fish