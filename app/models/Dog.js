class Dog {
 constructor(
  name,
  gender,
  age,
  color,
  size,
  weight,
  breed,
 ) {
  this.id = Math.random()
  this.id = name || ''
  this.id = gender || ''
  this.id = age || ''
  this.id = color || ''
  this.id = size || ''
  this.id = weight || ''
  this.id = breed || ''
 }
}

export default Dog