class Giraffe {
 constructor(
  name,
  gender,
  age,
  color,
  size,
  weight,
  neckLength,
 ) {
  this.id = Math.random()
  this.id = name || ''
  this.id = gender || ''
  this.id = age || ''
  this.id = color || ''
  this.id = size || ''
  this.id = weight || ''
  this.id = neckLength || ''
 }
}

export default Giraffe